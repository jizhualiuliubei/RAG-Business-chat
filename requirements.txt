# =========================================================
# 项目依赖（来自课程 requirements_full.txt，已安装在 conda 环境）
# 注意：不要在这里改版本，使用你 conda 环境已装好的
# =========================================================

# LangChain 核心
langchain==1.2.12
langchain-core==1.2.18
langchain-community==0.4.1
langchain-text-splitters==1.1.1

# LangGraph / Agent
langgraph==1.1.2

# 模型
langchain-openai==1.1.11
langchain-deepseek==1.0.1
openai==2.26.0

# 向量库
langchain-milvus==0.3.3
pymilvus==2.6.12

# 后端 API
fastapi==0.135.1
uvicorn==0.46.0
python-multipart  # 文件上传需要
pymysql==1.1.2      # MySQL 驱动，线上 DB_DRIVER=mysql 时需要

# 文档解析
pypdf==6.10.2
python-docx==1.2.0
Pillow==12.0.0
numpy==2.3.5
openpyxl==3.1.5
reportlab==5.0.1

# MCP（可选，P1）
mcp==1.27.0
fastmcp==3.2.4
langchain-mcp-adapters==0.2.1

# 配置
python-dotenv==1.2.1
